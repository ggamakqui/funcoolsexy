<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.koreait.fcs.dao.QNABoardDAO">
	<select id="qnaBoardList" resultType="com.koreait.fcs.dto.QNABoardDTO">
		SELECT * FROM (SELECT ROWNUM RN, A.* FROM (SELECT * FROM C_QNA WHERE PNO = #{param3} ORDER BY QREF DESC, QDEPTH ASC, QSTEP DESC) A)
		WHERE RN BETWEEN #{param1} AND #{param2}
	</select>
	
	<select id="getTotalRecord" resultType="int">
		SELECT COUNT(*)
		FROM C_QNA
	</select>
</mapper>